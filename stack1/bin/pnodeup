#!/bin/bash
# most ports just don't work (DNS name failure on the other side; no idea why) 9090 works
~/node_exporter*/node_exporter --web.listen-address="0.0.0.0:9090" > ~/cluster-test/stack1/node_exporter/logs/nodeexporter1.log 2>&1 &
